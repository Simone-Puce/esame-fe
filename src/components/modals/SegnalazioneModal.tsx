import { Modal } from "antd";
import { ReactElement } from "react";

const SegnalazioneModal = (): ReactElement => {
    <Modal title="Crea segnalazioni" open={creaSegnalazioneModal} onCancel={handleCancel} footer="">
    
}